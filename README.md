# Agent Configurations

These are my personal coding assistant and agent configurations.

## Installation

Just ask the agent to align the local configuration with the files
in the corresponding directory, `codex` or `claude`.
